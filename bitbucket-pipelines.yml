image: node:6.11.2
clone:
  depth: full

pipelines:
  default:
    - step:
        caches:
          - node
        script:
          - npm install
          - npm run lint
  branches:
    development:
      - step:
          caches:
            - node
          script:
            - npm install
#            - npm run lint
#            - npm run build
            - git push https://heroku:$HEROKU_API_KEY@git.heroku.com/$HEROKU_DEVELOPMENT_APP_NAME.git HEAD
    master:
      - step:
          caches:
            - node
          script:
            - npm install
            - npm run lint