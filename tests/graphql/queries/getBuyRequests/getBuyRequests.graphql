query getBuyRequests($textbookId: ID, $recipientId: ID){
    getBuyRequests(textbookId: $textbookId, recipientId: $recipientId) {
      id
      isAccepted
    	recipientId
    	textbookId
   	  userId
      message
    }
}
