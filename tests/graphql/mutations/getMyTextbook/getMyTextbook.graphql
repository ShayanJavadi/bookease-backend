query getMyTextbook($textbookId: ID!){
    getMyTextbook(textbookId: $textbookId){
        id,
        title,
        uid,
        description,
        edition,
        industryIdentifiers{
            type,
            identifier
        },
        images{
            thumbnail
        },
        authors,
        userId,
        createdAt,
        updatedAt,
        publishedAt,
        condition,
        price
    }
}